//import { string } from "prop-types";

//[field] inseamna ca poate fi de tipul firstname de ex

//nu se stie care set state se exec primul
// trebuie folosita fctie lambda mereu la set state
// folosire prevState dat ca param


//validate(field: string,text:string,regex: RegExp)



//cand e vb de returnat chestii de pus in render(<test>)nu prea se pun in clase diferite
export default class MyServices{
    //public getBalance{
//...fetch
    //}
}

export interface MyResponse{
    balance:number;
}

//se face import unde e nevoie
//se trimit stateurile ca param